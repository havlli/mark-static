---
title: Configuration
description: Configure site identity, content, theme, and deployment.
---

# Configuration

Edit `markstatic.config.js` to change the site name, description, content directory, theme preset, background, and production base path.

Edit `src/custom-theme.css` when you only need to override a few design tokens.
